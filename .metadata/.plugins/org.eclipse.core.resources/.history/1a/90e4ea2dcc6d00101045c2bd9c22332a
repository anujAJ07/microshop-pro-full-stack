package com.example.inventoryservice.messaging;

//This record defines the data we expect in an "OrderCreatedEvent" message
public record OrderConfirmedEvent(Long orderId, Long productId, int quantity) {
}
